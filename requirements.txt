# Runtime. The GUI/server/queue/updater are standard library; these are the extras.
deew>=2.9          # drives Dolby Encoding Engine (DEE itself is proprietary, not included)
pymediainfo>=6.0   # Atmos detection for the audio-encode task (Windows wheel bundles MediaInfo.dll)
# deezy (DDP Atmos) is NOT installed here: it pins rich>=14 while deew pins rich<14, so the two
# cannot share an environment. Use DeeZy's standalone exe and point Settings at it.
pywebview>=5.0     # native desktop window (falls back to the browser if unavailable)
