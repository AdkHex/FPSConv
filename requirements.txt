# Runtime. The GUI/server/queue/updater are standard library; these are the extras.
deew>=2.9          # drives Dolby Encoding Engine (DEE itself is proprietary, not included)
deezy>=1.3         # DDP Atmos: drives truehdd + DEE 5.2 (audio-encode task); brings pymediainfo for Atmos detection
pywebview>=5.0     # native desktop window (falls back to the browser if unavailable)
